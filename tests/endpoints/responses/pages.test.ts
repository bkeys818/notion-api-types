import type { Pages } from '../../../types/endpoints'
import { page } from '../../responses'

const createRes: Pages.Create.Response = page

const updateRes: Pages.Update.Response = page

const retrieveRes: Pages.Retrieve.Response = page
