import type { SelectOptions } from '../../../types/responses/properties/global'

export const selectOptions: SelectOptions = {
    name: 'some select option name',
    id: 'random_select_option_id',
    color: 'default',
}
